√ unique blogname (beirutspring)
√ access to db

-- get twitter username
-- ability to check if submitted blog exists
-- ability to preview RSS feed
-- if everything ok, allow to submit blog
-- when submitted, blog will be added to db, with crawling set to "inactive"
-- add admin interface to approve submitted blogs
-- hide everything until vue loads
-- add descriptions to each field for better UX